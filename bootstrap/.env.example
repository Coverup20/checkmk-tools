# CheckMK Bootstrap Configuration
# Copy this file to .env and customize for your environment

# ============================================
# System Configuration
# ============================================

# Timezone (e.g., Europe/Rome, America/New_York, UTC)
TIMEZONE="Europe/Rome"

# ============================================
# SSH Configuration
# ============================================

# SSH port (default: 22)
SSH_PORT=22

# Allow root login (yes/no/prohibit-password)
PERMIT_ROOT_LOGIN=no

# SSH keep-alive settings
CLIENT_ALIVE_INTERVAL=600
CLIENT_ALIVE_COUNTMAX=2
LOGIN_GRACE_TIME=30

# Root password (optional - leave empty to skip)
ROOT_PASSWORD=""

# ============================================
# Network Configuration
# ============================================

# Open HTTP (80) and HTTPS (443) ports in firewall
OPEN_HTTP_HTTPS=true

# ============================================
# NTP Configuration
# ============================================

# NTP servers (space-separated)
NTP_SERVERS="0.pool.ntp.org 1.pool.ntp.org 2.pool.ntp.org 3.pool.ntp.org"

# ============================================
# Let's Encrypt / Certbot Configuration
# ============================================

# Email for Let's Encrypt notifications
LETSENCRYPT_EMAIL=""

# Domains for SSL certificate (space-separated)
LETSENCRYPT_DOMAINS=""

# Web server (apache, nginx, standalone)
WEBSERVER=apache

# ============================================
# CheckMK Configuration
# ============================================

# CheckMK download URL
# Get the latest URL from: https://checkmk.com/download
# Example for Ubuntu 22.04 (Jammy):
CHECKMK_DEB_URL="https://download.checkmk.com/checkmk/2.2.0p15/check-mk-raw-2.2.0p15_0.jammy_amd64.deb"

# CheckMK admin password (cmkadmin user)
# Leave empty to keep default password (need to change on first login)
CHECKMK_ADMIN_PASSWORD=""

# ============================================
# Postfix Email Relay Configuration
# ============================================
# Note: This section is used by 25-postfix.sh
# If not configured, the script will ask interactively

# SMTP relay host (e.g., smtp.gmail.com:587)
RELAYHOST=""

# SMTP username
SMTP_USER=""

# SMTP password
SMTP_PASS=""

# ============================================
# Backup Configuration
# ============================================

# Backup directory
BACKUP_DIR="/var/lib/checkmk_backups"

# Backup retention in days
RETENTION_DAYS=30

# ============================================
# Notes
# ============================================

# 1. All values are optional except for interactive prompts
# 2. Empty values will use defaults or prompt for input
# 3. Keep this file secure (contains passwords)
# 4. Add .env to .gitignore to prevent committing secrets

# ============================================
# Example Configurations
# ============================================

# For Gmail SMTP:
# RELAYHOST="smtp.gmail.com:587"
# SMTP_USER="your-email@gmail.com"
# SMTP_PASS="your-app-password"

# For Office 365 SMTP:
# RELAYHOST="smtp.office365.com:587"
# SMTP_USER="your-email@company.com"
# SMTP_PASS="your-password"

# For SendGrid SMTP:
# RELAYHOST="smtp.sendgrid.net:587"
# SMTP_USER="apikey"
# SMTP_PASS="your-sendgrid-api-key"
